# Renomeie para .env e preencha com valores fortes (mínimo 32 caracteres aleatórios).
# Gere com:  python -c "import secrets; print(secrets.token_urlsafe(48))"

OSINT_JWT_SECRET=troque-isto-por-um-segredo-forte-de-pelo-menos-32-chars
OSINT_ADMIN_TOKEN=troque-isto-tambem-por-outro-segredo-forte

# Opcionais (defaults sensatos)
OSINT_JWT_TTL_MINUTES=60
OSINT_CACHE_TTL_SECONDS=3600
OSINT_CACHE_MAX_ENTRIES=1024
OSINT_RATE_LIMIT_PER_MINUTE=30
